// CRM Dashboard UI entry — shared hooks and components for embedding MightyOps

export { useLeads } from "./hooks/useLeads";
export { useDeals } from "./hooks/useDeals";
export { useNotifications } from "./hooks/useNotifications";
export type { UseLeadsOptions, UseDealsOptions } from "./types";

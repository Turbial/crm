# Current Sprint — Empty DB

**Goal:** Get data into the CRM. It's running but has no users and no data.

- Create owner user (`marcus@turbial.com`)
- Import Reach CRM contacts
- Verify login + pipeline view works

**Blockers:** None — Docker Compose running, frontend serving.
